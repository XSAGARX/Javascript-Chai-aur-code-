"use strict"; // it treats all js code as a newer versions. 

// alert (3 + 3) we are using node.js, not browser there is different syntax for alert.

console.log (17/

    21+ 

    12
    ); // you can write in this way but the code readability should be very high. 


    let age = 8;
    let isLoggedIn = false;
    let state = null;

    // number => 2 to 53
    // bigInt 
    // string => ""
    // boolean => true/false
    // null => standalone value
    // undefined => value is not assigned 
    // symbol => used mostly for uniqueness ex: ul/ux designer 

    console.log(typeof undefined);
    console.log(typeof null);
    